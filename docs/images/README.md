# README images

`faro-dashboard.png` is a screenshot of the public Vercel dashboard captured on
2026-09-20 at 1440 × 1000. It shows the existing simulated demo state; capture
did not reset the scenario, submit reports or trigger communications.

To refresh it, capture the dashboard after the map and state have loaded. Keep
the simulation indicator and map attribution visible. Review the image for
private information before committing it.

The main README also reuses the existing brand SVGs and wildfire-drills poster
from `public/`; those assets are not duplicated here.
