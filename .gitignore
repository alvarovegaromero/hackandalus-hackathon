# Local credentials; commit only sanitized templates.
.env
.env.*
!.env.example
!.env.*.example

# Personal agent settings and notes.
**/.claude/settings.local.json
**/CLAUDE.local.md

# Estado persistido en local (CRISIS_PERSISTENCE=on). Nunca se versiona.
.data/

# Regenerable code graph and OS metadata.
/graft/
.next/
.next.broken-*/
node_modules/
coverage/
.DS_Store
Thumbs.db

# Directorio de compilacion del segundo servidor dev (NEXT_DIST_DIR).
.next-dev/
